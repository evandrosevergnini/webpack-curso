// Adicionando loaders\
  : package.json
  > yarn add --dev css-loader style-loader

: webpack.config.js
    module: {
      rules: [
        {
          test: /\.css$/,
          use: ["style-loader", "css-loader"]
        }
      ]
    },
  } 

: /src/assets/css
  + mover o arquivo main.css e reset.css para /src/assets/css

: main.js
  + import "./assets/css/main.css";

: index.html
    + remover as importações de CSS que estão comentadas, pois não serão mais necessárias
    ! observar que o index.html gerado não tem os CSS, mas eles estáo incluídos no main-bundle.js e são injetados na index.html em tempo de execução
