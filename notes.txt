// Transpilando JavaScript para browsers antigos

: /src/assets/js
+ alterar o código de calculadora.js para
  export const soma = (a, b) => parseInt(a) + parseInt(b);

> yarn add --dev @babel/core @babel/preset-env babel-loader

:babel.config.json 
+ criar arquivo com o código
  {
    "presets": [
      "@babel/preset-env"
    ]
  }

: webpack.config.js
+ adicionar à propriedade rules de module
  {
    test: /\.js$/,
    exclude: /node_modules/,
    use: ["babel-loader"],
  },
